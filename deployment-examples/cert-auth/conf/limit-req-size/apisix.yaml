routes:
  - name: foo
    uri: /foo/*
    upstream:
      nodes:
        "echo-api.3scale.net:80": 1
      type: roundrobin
    plugins:
      limit-size:
        request_limit: 10
        request_limit_unit: bytes
        full_request: false
#END
