meta {
  name: wrong client id returns 401
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/any/jwt-wrong-id
}

tests {
  test("status is 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
