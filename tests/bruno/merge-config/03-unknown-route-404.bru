meta {
  name: Unknown route returns 404
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/not-a-route
}

headers {
  accept: application/json
}

tests {
  test("status is 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
}
