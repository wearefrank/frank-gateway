meta {
  name: Route from multiline fixture returns 200
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/from-multiline
}

headers {
  accept: application/json
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("response body is present", function() {
    const body = res.getBody();
    expect(body).to.not.be.null;
  });
}
