meta {
  name: wrong token endpoint returns 405
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/any/generic-wrong-endpoint
}

tests {
  test("status is 405", function() {
    expect(res.getStatus()).to.equal(405);
  });
}
