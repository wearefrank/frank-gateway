meta {
  name: wrong client secret returns 401
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/any/generic-wrong-secret
}

tests {
  test("status is 401", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
