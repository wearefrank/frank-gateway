# Third-party test-only images that have disappeared from their upstream
# registry before (tag/path removed). CI caches these as tarballs so a
# suite still runs from cache even if upstream becomes unavailable.
# Use a tag reference, not @sha256 digest: docker save/load only preserves
# RepoTags, so a digest-only reference can never be matched locally again
# after a load, defeating pull_policy: missing.
# registry.gitlab.com/.../open-ftv/pdp:v0.1.0 == sha256:500feae2ba403d79a2ec0c454da0f07a1dc4561db6191f3a2d307eae0c22be81
registry.gitlab.com/digilab.overheid.nl/ecosystem/ftv/open-ftv/pdp:v0.1.0
